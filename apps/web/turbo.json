{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "passThroughEnv": ["SESSION_SIGNING_SECRET", "SUPABASE_SERVICE_ROLE_KEY"]
    },
    "dev": {
      "passThroughEnv": ["SESSION_SIGNING_SECRET", "SUPABASE_SERVICE_ROLE_KEY"]
    },
    "test": {
      "passThroughEnv": ["SESSION_SIGNING_SECRET", "SUPABASE_SERVICE_ROLE_KEY"]
    }
  }
}
